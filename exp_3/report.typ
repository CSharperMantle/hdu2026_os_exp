#import "../assets/hdu-report-typst/template/template.typ": *

#show: project.with(
  title: [
    杭州电子科技大学\
    《操作系统课程实践》\
    实验报告\
  ],
  subtitle: [],
  class: "计算机科学英才班",
  department: "卓越学院",
  authors: "鲍溶",
  author_id: "23060827",
  date: datetime(year: 2026, month: 4, day: 10),
  cover_style: "hdu_report",
)

#show link: underline

#let paragraph(title, body) = [
  #strong[#title] #h(1em) #body
]

#toc()

#pagebreak()

= 实验目的

+ 了解进程与子进程的概念，理解 Linux 系统中进程创建与程序替换的机制；
+ 掌握管道、FIFO、POSIX 消息队列、共享内存等 IPC 机制的原理。
+ 掌握利用 POSIX API 在用户态实现 IPC 的方法，理解不同 IPC 机制的适用场景与优缺点。

= 实验内容

本实验旨在深入理解 Linux 操作系统的 IPC 机制，并综合运用多种 IPC 原语构建多进程应用程序。

实验的前半部分要求实现一个支持管道（`|` 操作）与输入输出重定向（如 `>`、`<`、`>>`、`fd>&fd` 等操作）的简单 Shell 模拟程序。该程序需具备命令行解析能力，能够识别管道操作符并正确建立输入输出管道，对各子进程进行正确的文件描述符重定向，为后续步骤提供基础。实验的后半部分要求分别使用 FIFO、POSIX 消息队列和 POSIX 共享内存实现多进程间通信。

= 实验方法

#paragraph([模拟 Shell 实现])[
  实现简单分词器处理用户输入，识别管道符、重定向操作符及文件描述符编号等语法单位。使用 `fork(2)` 为每个子命令创建子进程，通过 `pipe(2)` 建立双向通信通道，并使用 `dup2(2)` 将管道端映射至标准输入输出。使用 `execve(2)` 与其包装函数完成程序替换。内置命令（`cd`、`pwd`、`exit`）直接在父进程执行，不创建子进程。
]

#paragraph([聊天室消息结构])[
  聊天室实验中，发送的每条消息均符合@code:msg-regex 所示的正则表达式。每条消息不长于 512 字节。当且仅当一字节序列的前 512 字节匹配@code:msg-regex，该字节序列表示一条良定义的消息。消息由 ASCII RS 字符结尾，包含二或三个字段，每个字段间由 ASCII US 字符分隔。第一个字段为消息类型，第二个字段为发信者标识，第三个字段为可选的附加信息。
]

#code(
  ```txt
  ^(JOIN|LEAVE|MSG)\x1f[^\x1e\x1f]*(\x1f[^\x1e\x1f]*)?\x1e$
  ```,
  caption: [以正则形式表示的二进制消息格式]
) <code:msg-regex>

#paragraph([多端中心化聊天室实现])[
  如@figure:star-topo 所示，使用星型拓扑实现中心化聊天室，主机作为中心节点维护所有客户端的状态，客户端之间不直接通信。在 FIFO 实现中，主机创建控制管道监听所有客户端的接入请求，各客户端创建私有数据管道供主机广播消息；利用 `poll()` 系统调用检测管道可写性以识别僵尸客户端。MQ 实现中，主机持有接入队列 `/mychat-host`，各客户端持有私有接收队列 `/mychat-client-<pid>`；主机以阻塞方式等待消息，另有后台线程定期探测已退出的客户端。SHMEM 实现中，每个客户端与主机共享一段独立的内存区域，通过 POSIX 信号量 `sem_t` 实现生产者和消费者之间的同步与互斥；僵尸检测借助 `sem_timedwait(3)` 的超时机制实现。
]

#img(
  image("assets/star-topo.png", width: 60%),
  caption: [多端中心化聊天室拓扑示意图]
) <figure:star-topo>

#paragraph([双端点对点聊天室实现])[
  使用点对点拓扑实现一对一聊天，以达到任务书中使用 POSIX 共享内存实现双向通信的要求。使用生产者--消费者模式实现同步，两端互为对方的消费者，共享同一个单缓冲；同时还需防止第三人加入对话。对话发起方首先通过 `shm_open(3)` 创建共享内存区段，初始化五个信号量：

  - $#raw("client_present") = 1$，确保同一时刻最多只有一个客户端接入；
  - $#raw("turnstile") = 1$，实现当前发送方的互斥访问；
  - $#raw("empty") = 1$，表示缓冲区空闲；
  - $#raw("full_host") = 0$、$#raw("full_client") = 0$，分别作为两端的接收就绪信号。

  服务端与客户端使用如@code:p2p-server-send、@code:p2p-client-send、@code:p2p-server-receive、@code:p2p-client-receive 所示算法进行资源共享。
]

#grid(
  columns: (1fr, 1fr),
  rows: 2,
  align: top,
  gutter: 1em,
  [
    #code(
      ```txt
      procedure server_send() {
        P(turnstile)
        P(empty)
          [ Write data to buffer ]
        V(full_client)
        V(turnstile)
      }
      ```,
      caption: [服务端发送函数]
    ) <code:p2p-server-send>
  ],
  [
    #code(
      ```txt
      procedure client_send() {
        P(turnstile)
        P(empty)
          [ Write data to buffer ]
        V(full_server)
        V(turnstile)
      }
      ```,
      caption: [客户端发送函数]
    ) <code:p2p-client-send>
  ],
  [
    #code(
      ```txt
      procedure server_receive() {
        P(full_server)
          [ Read data from buffer ]
        V(empty)
      }
      ```,
      caption: [服务端接收函数]
    ) <code:p2p-server-receive>
  ],
  [
    #code(
      ```txt
      procedure client_receive() {
        P(full_client)
          [ Read data from buffer ]
        V(empty)
      }
      ```,
      caption: [客户端接收函数]
    ) <code:p2p-client-receive>
  ],
)

#paragraph([功能验证])[
  分别在三种 IPC 模式下启动主机进程与多个客户端进程，观察客户端加入、消息广播、客户端退出的行为是否符合预期。对每种 IPC 机制进行故障测试，涉及客户端收到信号退出、消息过长、客户端重名等场景，以验证帧协议解析、僵尸回收、进程退出资源清理等功能的正确性。
]

= 实验过程

== Zig 语言与问题规避

本实验使用 Zig 编程语言完成。Zig 是 Andrew Kelley 于 2016 年发明的底层通用编程语言@wikipedia2026zig，目前正处于活跃开发阶段。其特色为“更强大的 C 语言”，尝试在保留 C 语言低级思维方式的前提下增强表达能力。Zig 使用 LLVM 作为编译工具链，支持 C 调用约定的外部函数，原生支持链接至 libc，适合本实验使用。

Zig 标准库提供了部分 POSIX 接口，但是不包括消息队列、信号量等接口。为了避免 Zig 编译器在导入外部 C 头文件时出现段错误，本实验中为所有使用过的相关函数编写了包装函数，完成跨语言边界类型转换、`errno(3)` 到 Zig 原生错误类型的传播等底层操作。Zig 语言存在多种表示内存区域的类型，包括显式记录长度的 `[]T` 和隐式长度、以 `v` 值结尾的 `[:v]T`，以及它们的常量与指针变体。包装层还需要负责将这些类型的值转换至与 POSIX API 兼容的指针+长度形式。

为了便于与第三方库交互，Zig 语言提供了 `opaque` 类型@zig0152opaque，允许创建该类型的指针，语义类似 C 语言中的 `void *`，含义为“指向未知非零大小对象的指针”。但对于 `sem_t` 一类需要分配、对大小敏感的对象，`opaque` 无法分配表达正确的大小。本实验根据 glibc 中的定义@glibc243semt 编写了一个有大小的不透明结构体，以其作为内存分配的对象参考。

== 实现模拟 Shell

模拟 Shell 支持的文法如@eq:myshell-grammar 所示。文法起始符号为由管道符 `|` 分隔的多个命令序列。每个命令由单词序列组成命令名与参数，后缀若干重定向操作组成。实现的重定向操作包括重定向文件至标准输入（`<`）、重定向标准输出至文件（`>`）、重定向标准输出追加至文件（`>>`）及复制文件描述符（`>&`）。每个单词由非特殊字符组成，或为单引号或双引号包裹的特殊字符字面量。

#equation(
  [
    $
      #raw("<pipeline>")    &::= #raw("<command>") ( #raw("\"|\"") #raw("<command>") )^* \
      #raw("<command>")     &::= #raw("<word>")^* #raw("<redirection>")^* \
      #raw("<redirection>") &::= #raw("<fd>")^? ( #raw("\"<\"") | #raw("\">\"") #raw("\">\"")^? | #raw("\"&>\"") ) #raw("<word>") \
      #raw("<word>")        &::= ( #raw("<char>") - #raw("<meta>") | #raw("<quoted>") )^+ \
      #raw("<quoted>")      &::= #raw("\"'\"") #raw("<word>")^+ #raw("\"'\"") | #raw("\"\\\"\"") #raw("<word>")^+ #raw("\"\\\"\"") \
      #raw("<fd>")          &::= #raw("<digit>")^+ \
      #raw("<meta>")        &::= #raw("\" \"") | #raw("\"|\"") | #raw("\"<\"") | #raw("\">\"") | #raw("\"'\"") | #raw("\"\\\"\"")
    $
  ],
  caption: [模拟 Shell 支持的文法]
) <eq:myshell-grammar>

模拟 Shell 的工作流程分为分词、解析与执行三个主要阶段。分词阶段采用有限状态机处理引号，通过维护 `none`、`single`、`double` 三态识别单双引号上下文。解析阶段将词元序列组织为 `Pipeline` 与 `Command` 的层次结构。重定向以 `Redir` 联合体表示，支持文件重定向（`<`、`>`、`>>`）与文件描述符复制（`>&`），并以有序列表形式附于各命令，保证 `cmd 2>&1 >file` 等组合的求值顺序正确。解析器引入 `pending_redir` 中间状态，在识别到重定向操作符后延迟等待后续词元作为目标，同时从操作符前缀中提取可选的文件描述符编号，以支持 `2>file` 形式的重定向操作。

执行阶段采用 fork + exec 模型。对含 $N$ 个命令的管道，父进程预先创建 $N−1$ 个匿名管道，随后依次 `fork(2)` 各子进程：每个子进程以 `dup2(2)` 将前一级管道的读端映射至标准输入、将标准输出映射至本级管道的写端，然后按序执行命令的重定向列表，最后调用 `execve(2)` 完成程序替换。父进程在全部 `fork(2)` 完成后关闭所有管道端以确保子进程可正确感知 EOF，随后阻塞等待全部子进程退出。内建命令 `cd`、`pwd`、`exit` 若以单命令形式出现，则在父进程中直接执行，以保证 `cd` 对工作目录的变更对父进程生效；若出现于管道内部，则在子进程中执行，以支持读取当前工作目录场景下的管道 I/O 重定向。

验证过程涵盖单命令执行、管道串联、文件重定向及内建命令，均能够正确运行，如@figure:myshell-run 所示。

#img(
  image("assets/myshell-1.png"),
  caption: [myshell 功能验证]
) <figure:myshell-run>

== 多端聊天室实现

三种 IPC 实现共享“实验方法”节所述的帧协议，主机端均以客户端名称为键维护哈希表，记录各客户端的通信端点与存活状态。

FIFO 实现中，主机创建控制管道，接收所有客户端的接入请求与消息；各客户端在创建私有数据管道用于接收下行消息。主机以 250 ms 为超时周期通过 `poll(2)` 轮询控制管道，超时后对各客户端数据管道以非阻塞只写模式尝试打开，若失败则判定客户端已离线（成为”僵尸”），并触发”离开聊天室”消息广播。

消息队列实现中，主机持有一个 MQ，各客户端共享写入上行消息；各客户端均持有一个私有 MQ 接收下行消息。主机在主循环中使用 `mq_receive(3)` 以阻塞方式等待消息，在广播时使用非阻塞发送并记录每个客户端的失败次数。另有后台线程每 250 ms 遍历客户端队列若发现重试次数超过阈值则判定为僵尸。

共享内存实现中，主机和每个客户端各持有一段独立的共享内存区域，其中包含两个 POSIX 信号量（`data_sem` 初始化为 0，`space_sem` 初始化为 1）与一个 512 字节的帧缓冲区，构成生产者--消费者对。主机阻塞在自身区域的 `data_sem` 上等待客户端写入。由于共享内存不存在除超时之外的通知机制，主机在向各客户端广播时通过 `sem_timedwait(3)` 以 10 ms 超时等待 `space_sem`，连续三次超时后即判定客户端为僵尸。

#img(
  image("assets/mychat-fifo-1.png"),
  caption: [多端聊天室 FIFO 模式功能验证]
) <figure:mychat-fifo>

== 点对点聊天室的实现

点对点模式在服务端与单一客户端之间共享一段内存区域，包含五个信号量（`client_present`、`turnstile`、`empty`、`full_host`、`full_client`）与一个 512 字节的单向缓冲区。服务端首先通过 `client_present` 信号量（初始值为 1）确保同一时刻最多一个客户端接入，防止第三方抢占会话。

双向通信通过对称但方向相反的生产者--消费者算法实现：发送方依次等待 `turnstile` 和 `empty` 以获取缓冲区独占写权，写入后释放对端的就绪信号量（`full_host` 或 `full_client`）；接收方等待己方就绪信号量后读取数据并释放 `empty`。单一共享缓冲区配合两组方向信号量的设计在避免死锁的同时满足双向通信的需求。

#img(
  image("assets/mychat-p2p-1.png"),
  caption: [点对点聊天室 SHMEM 模式功能验证]
) <figure:mychat-p2p>

== 功能测试

聊天室程序的测试重点围绕协议正确性、并发条件下的消息传递行为以及异常退出后的健壮性展开。对于 FIFO、消息队列与共享内存三种多端模式，测试均覆盖客户端加入与离开、主机接收上行消息、消息向其余客户端广播、多客户端并发发送时的消息完整性，以及客户端被强制终止后主机对僵尸客户端的探测与回收等方面；其中并发场景通过多个客户端同时连续发送消息，验证主机日志中消息条数与内容是否完整一致，并检查是否出现帧解析错误。对于点对点模式，测试则进一步考察单客户端排他接入约束是否成立，以及在共享内存半双工缓冲区上双向消息传递是否能够按预期完成，从而验证同步模式的正确性。

测试方法上，在 `exp_3/tests` 目录中编写了一组 shell 脚本，以黑盒方式驱动主机与客户端进程。测试脚本通过后台启动进程、使用临时 FIFO 向标准输入注入消息，并轮询各进程日志中的 `Joined:`、`Left:`、`Reaped zombie client:` 等关键词与具体消息内容来判断行为是否符合预期；统一入口脚本 `run.sh` 负责依次执行全部测试并汇总结果。对于多端模式，脚本分别构造基本接入与退出、广播、并发压力以及 `SIGKILL` 异常退出等场景；对于点对点模式，则构造单连接占用与双向收发场景，观察第二个客户端在会话占用期间无法完成接入，以及双方消息均可被对端正确接收。通过上述测试，可以较为系统地验证聊天室程序在正常流程与异常流程下的功能完整性与同步机制正确性，如@figure:test-result 所示。

#img(
  image("assets/test-result.png"),
  caption: [端到端测试结果]
) <figure:test-result>

= 结果分析与实验体会

实验最终实现了全部预期功能：模拟 Shell 在单命令、管道串联与 I/O 重定向等场景下均可正确执行，内建命令与外部命令的混合管道也能正常工作；多端聊天室在 FIFO、消息队列与共享内存（星型）三种模式下均可正常处理多客户端加入、消息广播与退出，在客户端进程意外终止时僵尸检测与资源回收机制同样能够触发；点对点聊天室的双向通信与互斥准入机制运行正确，第三方无法在会话期间抢占接入。

通过对比三种 IPC 机制，可以归纳出各自的工程权衡：FIFO 实现最为直观，借助 Linux 对管道端可写性的内核语义即可简洁实现僵尸探测，但主机仅有单一控制管道，大量并发客户端时可能成为瓶颈；消息队列提供消息原子性保证，主机代码因此无需手动处理帧边界的拼接问题，但需额外后台线程进行僵尸探测，且绑定 libc 接口的工程摩擦较高；共享内存的访问延迟最低，但同步逻辑最为复杂，信号量的初值与等待顺序需要仔细设计以排除死锁。

本次实验加深了我对几个操作系统核心概念的理解。在上学期的理论课课程中，我们研究过“双向车流共享单车道桥梁”的场景，该问题的解经过稍微修改后即可用于点对点模式的同步方案。我还了解了不同 IPC 原语在缓冲语义、持久性与接口复杂度上的差异，使我对“针对场景选择合适机制”这一设计原则有了更具体的认识。

= 源代码

本报告源文件可以从 #link("https://github.com/CSharperMantle/hdu2026_os_exp/tree/main/exp_3") 处获取。

#pagebreak()

#bibliography("bib.bib", style: "gb-7714-2015-numeric")
